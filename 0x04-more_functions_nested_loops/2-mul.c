#include "main.h"
#include <stdio.h>
/**
 * mul -multiply 2 numbers
 * @a: - first number
 * @b: - the second number
 * Return: 0
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
