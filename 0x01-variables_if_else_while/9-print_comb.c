#include <stdio.h>
/**
 * main - print all combinations
 * Return: 0
 */

int main(void)
{
	for (int i = 0; i < 10; i++)
		putchar(i + '0');
		putchair(',');
		putchar(' ');
	putchar('\n');
	return (o);
}
